<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    android:layout_width="match_parent"
    android:layout_height="68dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_row_cardView"
    android:orientation="horizontal"
    app:cardCornerRadius="10dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    app:cardBackgroundColor="@color/backgroundProducto"
    android:layout_marginBottom="4dp"
    >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:background="?android:attr/selectableItemBackground"
        android:gravity="center">
        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/shape_naranja"
            android:tint="@color/conectado"
            />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Manolito"
            android:textSize="28dp"
            android:textColor="@color/colorProducto"
            android:layout_marginHorizontal="8dp"/>
        <ImageView
            android:id="@+id/opciones"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_settings"
            android:tint="@color/colorProducto"
            />
        <ImageView
            android:id="@+id/borrar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_delete_black_24dp"
            android:tint="@color/colorProducto"
            />
    </LinearLayout>

</android.support.v7.widget.CardView>
